// export { auth as middleware } from "@/auth";

export function middleware() {
  // Empty middleware for now
}

export const config = {
  matcher: [] // Empty matcher means no routes will be processed
};
